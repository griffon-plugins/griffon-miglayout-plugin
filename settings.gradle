include 'subprojects/griffon-miglayout-javafx'
include 'subprojects/griffon-miglayout-javafx-groovy'
include 'subprojects/griffon-miglayout-swing-groovy'
include 'subprojects/griffon-miglayout-guide'

rootProject.name = 'griffon-miglayout-plugin'
rootProject.children.each { project ->
    int slash = project.name.indexOf('/')
    String fileBaseName = project.name[(slash+1)..-1]
    String projectDirName = project.name
    project.name = fileBaseName
    project.projectDir = new File(settingsDir, projectDirName)
    project.buildFileName = "${fileBaseName}.gradle"
    assert project.projectDir.isDirectory()
    assert project.buildFile.isFile()
}
